# Server
PORT=3000
NODE_ENV=production

# Admin access — REQUIRED. Use a long random string (32+ characters).
# Example: openssl rand -hex 32
ADMIN_TOKEN=change_me_to_a_long_random_secret_at_least_32_chars
